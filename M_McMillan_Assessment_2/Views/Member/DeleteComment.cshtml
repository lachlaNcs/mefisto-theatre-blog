@model M_McMillan_Assessment_2.Models.Comment

@{
    ViewBag.Title = "DeleteComment";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.Content)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Content)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DatePosted)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DatePosted)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-outline-secondary mt-2 ms-2" />
            @Html.ActionLink("Back to Post", "Details", "Home", new { id = Model.PostId }, htmlAttributes: new { @class = "btn btn-outline-secondary mt-2 ms-2" })
        </div>
    }
</div>
